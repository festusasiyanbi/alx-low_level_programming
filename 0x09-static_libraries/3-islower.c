#include "main.h"
/**
 * _islower - This program checks for lowercases
 * @c: this character checks if it's in lowercase
 *
 * Return: Returns 1 on success
 */
int _islower(int c)
{
if (c >= 'a' && c <= 'z')
return (1);
else
{
return (0);
}
}
