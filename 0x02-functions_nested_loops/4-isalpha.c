#include "main.h"
/**
 * _isalpha - checks if the char is in lower or uppercase
 * @c: this charater checks if it is in lowercase 
 *
 * Return: returns 1 on success
 */
int _isalpha(int c)
{
if (c >= 'a'; && c <= 'z';) || (c >= 'A'; && c <= 'Z';)
return (1);
else
{
return (0);
}
}
