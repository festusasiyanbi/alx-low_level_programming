#include <stdio.h>
/**
 * main - Entry for the program
 *
 * Return: Always return zero (success)
 */
int main(void)
{
char i;
for (i = 'a'; i <= 'z'; i++)
{
putchar(i);
}
putchar('\n');
return (0);
}
