#include <stdio.h>
/**
 * main - Entry for the program
 *
 * Return: Always return zero on success
 */
int main(void)
{
int i;

char hexavalues[] = '0123456789abcdef';

for (i = '0', i <= '16'; i++)
{
putchar([i]);
}
putchar('\n');
return (0);
}
