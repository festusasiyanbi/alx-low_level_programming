#include "main.h"
#include <stdio.h>
/**
 * main - the function prints multiplication of two integers
 * @argc: argument count
 * @argv: argument vector
 * Return: returns 0 if true otherwise 1
 */

int main(int argc, char *argv[])
{
int a, b;
if (agrc == 1)
{
a = atoi(argv[1]);
b = atoi(argv[2]);
printf("%d\n", a *b);
return (0);
}
printf("Error\n")
return (1);
}
